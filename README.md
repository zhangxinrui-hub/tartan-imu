# Tartan IMU 实验仓库

- 代码与脚本：评估、LoRA/车头训练、EKF 等。
- **`Drone_Dataset1/` 未纳入 Git**（体积过大，约 36GB）。若需复现无人机实验，请从实验室硬盘或网盘单独拷贝该目录。
- 权重文件（如 `checkpoint_28.pt`）已包含在仓库中；建议使用 Conda 环境并安装 PyTorch（可参考你本机 `tartan_gpu` 环境）。
